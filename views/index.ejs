<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Movies Watchlist</title>
    <link rel="stylesheet" href="styles.css">
</head>

<body>
    <h1>Movie Watchlist</h1>
    <h3>Movies</h3>
    <ul class="movies">
        <% for (let i=0; i<movies.length; i++) {%>
            <li class="movie">
                <form>
                    <span class="movie-title"><%= movies[i].title %></span>
                    <input type="text" class="title-input editable-inputs" value="<%= movies[i].title %>">
                    <span class="movie-genre"><%= movies[i].genre %></span>
                    <input type="text" class="genre-input editable-inputs" value="<%= movies[i].genre %>">
                    <span class="movie-year"><%= movies[i].year %></span>
                    <input type="text" class="year-input editable-inputs" value="<%= movies[i].year %>">
                    <span>
                        <i class="fa-regular fa-pen-to-square edit-btn" aria-hidden="true"></i>
                    </span>
                    <span>
                        <i class="fa-solid fa-trash-can trash-btn" aria-hidden="true"></i>
                    </span>
                    <button class="submit-btn">Submit</button>
                </form>
            </li>
        <% } %>
    </ul>
    <h2>Add Movies you want to Watch</h2>
    <form action="/movies" method="POST">
        <input type="text" placeholder="Movie Title" name="title">
        <input type="text" placeholder="Movie Genre" name="genre">
        <input type="text" placeholder="Release Year" name="year">
        <!-- <input type="hidden" name="movieWatched" value="false">
        <input type="checkbox" name="movieWatched" value="true"> Watched? -->
        <button type="submit">Add Movie</button>
    </form>
    <script src="https://kit.fontawesome.com/aabdb7c1a3.js" crossorigin="anonymous"></script>
    <script src="main.js"></script>
</body>

</html>